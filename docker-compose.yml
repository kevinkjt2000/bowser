version: '2.1'
services:
  bowser:
    build: .
    volumes:
      - .:/root/bowser
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    sysctls:
      - net.core.somaxconn=511

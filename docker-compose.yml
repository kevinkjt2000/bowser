version: '2.1'
services:
  bowser:
    build: .
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - redis
    restart: unless-stopped
  redis:
    image: "redis:alpine"
    sysctls:
      - net.core.somaxconn=511
    restart: unless-stopped
